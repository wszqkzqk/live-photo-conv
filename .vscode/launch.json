{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/builddir/src/live-photo-conv",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
